using InteractionSystem.Interactions;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace InteractionSystem.Interactables.Buildings
{
    [RequireComponent(typeof(Interaction_Destroy))]
    [RequireComponent(typeof(Interaction_Heal))]
    [RequireComponent(typeof(Interaction_Upgrade))]
    public class Building : WorldEntity
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}

